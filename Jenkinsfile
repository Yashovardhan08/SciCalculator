pipeline {
    agent any
    environment {
        PATH = "/opt/homebrew/bin/mvn:$PATH"
    }
    stages {
        stage('build') {
            steps {
                sh 'echo "JEELL0O"'
            }
        }
    }
}