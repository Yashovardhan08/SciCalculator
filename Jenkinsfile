pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'java --version'
            }
        }
        stage('build2') {
        steps {
                 sh 'mvn --version'
            }
        }
    }
}